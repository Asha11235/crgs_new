<!DOCTYPE html>

<html>
<head>
    <meta charset="utf-8">
    <title>&{'application.name'} &raquo; #{get 'title' /}</title>

    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="">
    <meta name="author" content="">

    <!-- For HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
    <script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>
    <![endif]-->

	<!-- For fav icons -->
    <link rel="shortcut icon" type="image/png" href="@{'/public/images/favicon.png'}">

    <!-- For styles -->
	#{stylesheet 'bootstrap.min.css' /}
	#{stylesheet 'bootstrap-responsive.min.css' /}
	#{stylesheet 'main.css' /}
    <!-- Uniform -->
    #{stylesheet 'uniform.default.css' /}

    #{get 'moreStyles' /}

    #{script 'jquery-1.7.2.min.js' /}

       
    #{stylesheet 'fancybox/jquery.fancybox-1.3.4.css' /}
    #{get 'moreStyles' /}

</head>
